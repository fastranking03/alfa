<!DOCTYPE html>
<html lang="en">
<head>
    <title>User Profile</title>
    <%- include('includes/head') %>
</head>
<body>
    <h1>User Profile</h1>
    <header>
        <% if (user) { %>
            <p>Welcome, <%= user.name %>!</p>
        <% } else { %>
            <p>Welcome, Guest!</p>
        <% } %>
    </header>
    <a href="/logout">Logout</a>
</body>
</html>
