<!DOCTYPE html>
<html lang="en">

<head>

    <base href="https://alfa-menswear.com/" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Alfa Admin | Add Staff</title>
    <%- include('includes/head') %>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body>
    <div class="main-admin-wrapper">
        <%- include('includes/header') %>
            <div class="alfa-admin-wrapper">
                <div class="admin-side-nav">
                    <%- include('includes/sidebar') %>
                        <div class="admin-main-layout">
                            <div class="admin-breadcrumb">
                                <div class="admin-breadcrumb-title">
                                    <h4>
                                        <%= staff ? 'Update Staff' : 'Add Staff' %>
                                    </h4>
                                </div>
                            </div>
                            <div class="admin-card">
                                <!-- Add Staff Form -->
                                <form action="admin/staff-details-submit" method="POST" class="row g-3">

                                    <input type="hidden" name="role" value="staff">
                                    <% if (staff && staff.id) { %>
                                        <input type="hidden" name="id" value="<%= staff.id %>" readonly>
                                        <% } %>
                                            <div class="col-md-12">
                                                <label for="name" class="form-label">Name:</label>
                                                <input type="text" id="name" name="name" class="form-control"
                                                    value="<%= staff ? staff.name : '' %>" required>
                                            </div>
                                            <div class="col-md-12">
                                                <label for="email" class="form-label">Email:</label>
                                                <input type="email" id="email" name="email" class="form-control"
                                                    value="<%= staff ? staff.email : '' %>" required>
                                            </div>
                                            <div class="col-md-12">
                                                <label for="password" class="form-label">Password:</label>
                                                <input type="text" id="password" name="password" class="form-control"
                                                    value="<%= staff ? staff.password : '' %>" required>
                                            </div>
                                            <div class="col-md-12">
                                                <label for="current_status" class="form-label">Current Status:</label>
                                                <select id="current_status" name="current_status" class="form-control">
                                                    <option value="active" <%=staff && staff.current_status==='active'
                                                        ? 'selected' : '' %>>Active</option>
                                                    <option value="inactive" <%=staff &&
                                                        staff.current_status==='inactive' ? 'selected' : '' %>>Inactive
                                                    </option>
                                                </select>
                                            </div>
                                            <div class="col-12" style="text-align: right;">
                                                <button type="submit" class="submit-btn">
                                                    <%= staff ? 'Update Staff' : 'Add Staff' %>
                                                </button>
                                            </div>
                                </form>
                            </div>
                        </div>
                </div>
            </div>
    </div>
    <%- include('includes/foot') %>
</body>

</html>